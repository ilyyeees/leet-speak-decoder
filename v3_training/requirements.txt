# V3 Training Requirements
# =========================
# Full requirements for the v3 training pipeline
# Optimized for 2x RTX 5090 (32GB VRAM each)
#
# Install with: pip install -r requirements.txt

# Core ML Stack
torch>=2.1.0
transformers>=4.36.0
datasets>=2.16.0
accelerate>=0.25.0
sentencepiece>=0.1.99
huggingface_hub>=0.20.0  # For downloading v2 model to RAM

# Evaluation
evaluate>=0.4.0
sacrebleu>=2.4.0
jiwer>=3.0.0  # For CER/WER

# Training Utilities
tqdm>=4.66.0
numpy>=1.24.0

# Logging & Monitoring
tensorboard>=2.15.0
wandb>=0.16.0  # Optional: for experiment tracking

# Data Processing
requests>=2.31.0
orjson>=3.9.0  # Faster JSON parsing

# Optional: For DeepSpeed
# deepspeed>=0.12.0

# Optional: For Flash Attention (A100/H100)
# flash-attn>=2.4.0
